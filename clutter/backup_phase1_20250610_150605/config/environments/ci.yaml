# CI/CD Environment Configuration
environment: ci_cd
governance:
  base_threshold: 0.6
  fail_on_violations: true
  isolation_enabled: true

reporting:
  level: "detailed"
  format: "structured"
  export_artifacts: true
